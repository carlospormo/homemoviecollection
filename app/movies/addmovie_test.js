'use strict';

describe('moviesApp.add module', function() {

    beforeEach(module('moviesApp.add'));

    describe('add controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var addCtrl = $controller('AddCtrl');
            expect(addCtrl).toBeDefined();
        }));

    });
});